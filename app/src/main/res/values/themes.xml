<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.TourismApp" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/naranja</item>
        <item name="colorPrimaryVariant">@color/naranja</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Settea el theme a solo light -->
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>
        <!-- Color del fondo de todas las pantallas -->
        <item name="android:windowBackground">@color/background_color</item>
        <!-- Color de la fuente de todas las palabras -->
        <item name="android:textColor">@color/color_font</item>
    </style>
</resources>